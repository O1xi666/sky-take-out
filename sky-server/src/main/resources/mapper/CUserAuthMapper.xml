<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.CUserAuthMapper">

    <select id="getByUsername" resultType="com.sky.entity.User">
        select *
        from user
        where name = #{username}
        limit 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user
        (name, id_number, create_time)
        values
            (#{name}, #{idNumber}, #{createTime})
    </insert>

    <select id="getByUsernameAndPassword" resultType="com.sky.entity.User">
        select *
        from user
        where name = #{username}
          and id_number = #{password}
        limit 1
    </select>
</mapper>
